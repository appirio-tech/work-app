a.login(href="#/")
  .icon(ng-hide="vm.loggedInUser")
  .label(ng-click="signin()" ng-hide="vm.loggedInUser") Log in
  .label(ng-click="signout()" ng-show="vm.loggedInUser") {{vm.loggedInUser.handle}}
  .icon(ng-show="vm.loggedInUser")
