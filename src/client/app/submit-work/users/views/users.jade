.users
  h2 Your App's Users

  form(name="usersForm")
    label(for="your-app-users") Who are your app's users and how will they use your app?

    textarea#your-app-users(name="usageDescription", ng-model="vm.work.usageDescription", maxlength="300")

  ng-messages(for="vm.validate()" ng-hide="usersForm.usageDescription.$pristine && !vm.work.submitAttempted")
    ng-message(when="required") This field is required
    ng-message(when="minlength") Help our designers understand your needs, please enter at least 200 characters.

    button.submit(ng-click="vm.next()") Next
