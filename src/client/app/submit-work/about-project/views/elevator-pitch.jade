header(role="heading")
  h1 About your project

main
  form
    label(for="project-elevator-pitch") Give us a brief explanation or "Elevator Pitch"
    textarea#project-elevator-pitch(placeholder="My Project", ng-model="vm.work.summary")

    button.submit(type="submit", ng-click="vm.next()") Next

nav(role="navigation", "ng-submit-work-nav"=true)
