h2 About your project

h3 What type of project do you need?

form(name="typeForm", ng-submit="submit()")
  ul.project-types
    li
      label(for="project-type-design") DESIGN

      button#project-type-design(type="button", ng-class="{'selected': vm.work.requestType == 'design'}", ng-click="vm.work.requestType = 'design'")
        .starting-at starting at

        .price $2,000

        .duration about 1 week

      .description I need design for my app.

    li
      label(for="project-type-code") CODE

      button#project-type-code(type="button", ng-class="{'selected': vm.work.requestType == 'code'}", ng-click="vm.work.requestType = 'code'")
        .starting-at starting at

        .price $5,000

        .duration about 2 weeks

      .description I have a design and need an app created from it.

    li
      label(for="project-type-design-code") DESIGN &amp; CODE

      button#project-type-design-code(type="button", ng-class="{'selected': vm.work.requestType == 'both'}", ng-click="vm.work.requestType = 'both'")
        .starting-at starting at

        .price $10,000

        .duration about 3 weeks

      .description I have an idea for an app and need it designed and built.

    button.submit(type="submit", ng-show="vm.work.requestType") NEXT

