h2 What features does your app need?

h3 Choose features and describe it.

form
  ul
    li(ng-repeat="feature in vm.work.features")
      input(id="{{'feature-' + feature.id}}", type="checkbox", ng-model="feature.selected")

      .label-example
        label(for="{{'feature-' + feature.id}}") {{feature.name}}

        a(href='#') (example)

      .desc {{feature.description}}

      textarea(placeholder="explain briefly (optional)", ng-show="feature.selected", ng-model="feature.explanation")

  button.submit(type="submit", ng-click="vm.next()") Next

.example.layout-modal

  a.close(href="#", onclick)

style.
  /* need to remove this when integrating with controller */
  .overlay, .layout-modal {
    display: none;
  }
