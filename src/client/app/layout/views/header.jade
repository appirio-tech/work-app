header.layout-header(role="banner" layout-header=true)
  a.clean.logo("ng-home-link"=true, "href"="{{getPath()}}") Appirio Services Platform

  .secondary
    a.clean.login(href="#/login" login)

    button.clean.message-notifications(type="button" focus-on-click=true ng-show="unreadCount > 0") {{ unreadCount }}

    .popup
      threads



