header
  h5 Requirements

  hr.biggest

main
  h4(ng-if="vm.project.files.requirements.length") Uploaded design documents

  ul.files
    li(ng-repeat="file in vm.project.files.requirements")
      a(ng-if="file.downloadUrl" href="{{file.downloadUrl}}" target="_blank") {{file.filename}}