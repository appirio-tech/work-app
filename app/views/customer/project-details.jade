layout-header.elevated(role="banner" work-id="{{ vm.id }}")

layout-project-nav(work-id="{{ vm.id }}")

main.layout-main.project-page("role"="main" flush-height=true)
  h2 Project requirements

  p To make changes to your project requirements, contact your co-pilot.

  project-details(project-id="{{ vm.id }}")

layout-footer
