layout-header.elevated(role="banner" work-id="{{ vm.projectId }}" user-type="{{ vm.userType }}")

layout-project-nav(ng-if="vm.userType != 'member'" work-id="{{ vm.projectId }}" user-type="{{ vm.userType }}")

main.layout-main("role"="main" layout-main=true flush-height=true)
  submissions(ng-if="vm.stepType == 'designConcepts' || vm.stepType == 'completeDesigns'" project-id="{{ vm.projectId }}" step-id="{{ vm.stepId }}" step-type="{{ vm.stepType }}" user-type="{{ vm.userType }}")

  final-fixes(ng-if="vm.stepType == 'finalFixes' " project-id="{{ vm.projectId }}" step-id="{{ vm.stepId }}" user-type="{{ vm.userType }}")

layout-footer