layout-header(role="banner" work-id="{{ vm.workId }}" user-type="copilot")

layout-project-nav(work-id="{{ vm.workId }}" user-type="copilot")

main.layout-main.project-page("role"="main" layout-main=true flush-height=true)
  h1 Status Reports

  status-reports(work-id="{{ vm.workId }}" step-id="{{vm.stepId}}" flush-height=true)

layout-footer