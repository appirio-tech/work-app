layout-header.elevated(role="banner")

layout-project-nav(work-id="{{ vm.workId }}")

main.layout-main.final-fixes("role"="main" layout-main=true)
  final-fixes(project-id="{{ vm.projectId }}", step-id="{{ vm.stepId }}")

layout-footer
