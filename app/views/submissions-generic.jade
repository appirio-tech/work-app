layout-header(role="banner")

layout-project-nav(work-id="{{ vm.workId }}")

main.layout-main("role"="main" layout-main=true)
  submissions(ng-if="vm.stepType == 'designConcepts' || vm.stepType == 'finalDesigns' " project-id="{{ vm.projectId }}", step-id="{{ vm.stepId }}", step-type="{{ vm.stepType }}")

  final-fixes(ng-if="vm.stepType == 'finalFixes' " project-id="{{ vm.projectId }}", step-id="{{ vm.stepId }}")

layout-footer