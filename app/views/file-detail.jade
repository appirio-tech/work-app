layout-header(role="banner")

main.layout-main("role"="main" layout-main=true)
  file-detail(project-id="{{ vm.projectId }}", step-id="{{ vm.stepId }}", submissions-id="{{ vm.submissionsId }}", file-id="{{ vm.fileId }}")

layout-footer
