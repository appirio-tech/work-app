layout-header.elevated(role="banner")

main.layout-main("role"="main" layout-main=true)
  file-detail(project-id="{{ vm.projectId }}", step-id="{{ vm.stepId }}", submission-id="{{ vm.submissionId }}", file-id="{{ vm.fileId }}", isModal="{{ vm.isModal }}")

layout-footer
