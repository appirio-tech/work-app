layout-header.elevated(role="banner" work-id="{{ vm.projectId }}" user-type="{{ vm.userType }}")

layout-project-nav(ng-if="vm.userType != 'member'" work-id="{{ vm.projectId }}" user-type="{{ vm.userType }}")

main.layout-main.project-page.submissions("role"="main" layout-main=true flush-height=true)
  submission-detail(project-id="{{ vm.projectId }}", step-id="{{ vm.stepId }}", submission-id="{{ vm.submissionId }}" user-type="{{ vm.userType }}")

layout-footer
