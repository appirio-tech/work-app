layout-header(role="banner" work-id="{{ vm.projectId }}" user-type="{{ vm.userRole }}")

layout-project-nav(ng-if="vm.userRole != 'member'" work-id="{{ vm.projectId }}" user-type="{{ vm.userRole }}")

main.layout-main.project-page.submissions("role"="main" layout-main=true flush-height=true)
  submission-detail(
    project-id="{{ vm.projectId }}"
    step-id="{{ vm.stepId }}"
    submission-id="{{ vm.submissionId }}"
    user-type="{{ vm.userRole }}"
    permissions="vm.permissions"
  )

layout-footer
